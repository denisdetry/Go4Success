import { Tabs } from "expo-router";

export default function WorkshopsLayout() {
    return (
        <Tabs
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
